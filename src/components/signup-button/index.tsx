import Button from '@mui/material/Button';

const SignUpButton = () => (
  <span style={{ margin: '16px 0' }}>
    <Button
      className="button"
      variant="text"
      color="primary"
      size="small"
    >
      Criar conta
    </Button>
  </span>
)

export default SignUpButton;